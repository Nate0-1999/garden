packet:            S2 — Memory CRUD + dedup bands
session:           codex / 2026-07-17 / c842
status:            DONE
what exists now:   Spine POST/PATCH/GET `/v1/memories` are live with exact
                   conflict/problem media types, atomic root/CAS history, and
                   stable filtered paging. Create implements label-first checks,
                   0.92/0.80 cosine bands, force semantics, and per-principal
                   transaction serialization. The production OpenAI embedding
                   adapter is injectable, validates 1536-dimensional non-zero
                   vectors, and deterministic vectors remain test-only.
deviations:        DECISIONS 005 (P1.1) and 006 (P1.4, P1.1); enacted Garden
                   A-002 through A-006 to complete error, dedup, PATCH, list,
                   and label/body-limit semantics.
evidence:          Spine `verification/s2/README.md`; 52 pytest checks passed
                   against disposable pgvector Postgres; Ruff lint/format,
                   uv lock, scope fence, Compose config, and production Docker
                   build passed. Harness: 27 passed, one intentional H2 skip;
                   Ruff lint/format and scope fence passed.
notes to the next agent:  S3 is the next board packet. Reuse the provider and
                   app/session injection seams; `/v1/search` and all inject /
                   feedback routes intentionally remain 501. Create's advisory
                   lock covers one principal across workers and the authoritative
                   label/dedup recheck is inside that transaction. PATCH must keep
                   using S1's CAS helper. `cl100k_base` is loaded lazily on the
                   first body validation. Compose maps host `OPENAI_API_KEY` to
                   `SPINE_OPENAI_API_KEY`; without it, memory writes return a
                   sanitized RFC7807 503 while health/read routes still work.
