packet:            H1 — Envelope + daemon WS
session:           codex / 2026-07-20 / 25ff
status:            DONE
what exists now:   Harness validates the exact closed C.7 outer envelope and
                   routes every one of its eight message types through a copied
                   per-app async handler table. Handlers receive only a
                   validated envelope and sender and may emit multiple
                   envelopes; unregistered types preserve P0's same-shaped
                   `error: not implemented` fallback. Invalid text, non-object,
                   schema-invalid, hostile parser-limit, and binary frames
                   close deterministically before dispatch. Unknown WebSocket
                   paths also close before the root HTTP static mount.
deviations:        Garden A-013 completes malformed inbound framing and close
                   semantics; Harness DECISIONS 004 (P3) records the internal
                   routing seam. No FLAG or downstream payload law was added.
evidence:          Harness verification/h1/README.md; 50 passed with the one
                   intentional H2 contract skip, 35 focused H1 tests, Ruff
                   lint/format, uv lock, scope fence, web lint/build, a real
                   Uvicorn + websockets probe, and three independent read-only
                   audits. Spine exit ground: 158 passed against disposable
                   pgvector Postgres.
notes to the next agent:  Harness remote head is
                   2da8c7be4f2529330a9e4837b927e74e9a9db6eb. C.7 remains
                   deliberately silent on per-type payloads, directionality,
                   acknowledgements, and correlation; later packets must not
                   infer those from the fallback echo. Register H4/H5 behavior
                   by supplying MessageType-keyed handlers to create_app; keep
                   the exact `/ws` route before the catch-all and root static
                   mount. H2 is the next eligible packet by board order and
                   should replace the intentional live-Spine contract skip.
                   D1 remains human-owned and IN_PROGRESS. Stop here under the
                   one-packet rule.
