packet:            S6 — /v1/search
session:           codex / 2026-07-19 / f6a2
status:            DONE
what exists now:   Spine's final 501 is replaced by authenticated semantic
                   search over current ACTIVE heads. It embeds once, scopes by
                   principal and optional global-or-exact project context,
                   orders raw cosine then memory UUID, and returns the shared
                   SimilarityMemoryCard with null features/rank. k is strictly
                   bounded 1..50 and provider failures are RFC7807 503.
deviations:        DECISIONS 012 (P1.1), under enacted Garden A-012.
evidence:          Spine verification/s6/README.md; 82 pytest checks passed
                   against disposable pgvector Postgres through migration
                   0002. Ruff lint/format, uv lock, scope fence, Compose config,
                   committed OpenAPI freshness, zero-501 fence, and production
                   Docker image passed. Harness: 27 passed with the intentional
                   H2 live-contract skip; Ruff lint/format, uv lock, and scope
                   fence passed. Two independent read-only audits found no
                   blocker.
notes to the next agent:  S6 remote head is
                   10c6b932d5913dfd54ea772e48e84e84ac13ddc7. H2 must mirror
                   A-012's k bounds and turn its live Spine contract skip into
                   assertions; S6 now satisfies H3's added dependency. Omitted
                   or null project_key is principal-wide; only a non-null key
                   filters to global plus exact project. Search deliberately
                   ignores scorer threshold/weights/bias/pin priority and keeps
                   negative cosine. The required UUID tie-break can lead
                   PostgreSQL to choose an exact sort instead of HNSW as the
                   order-producing plan; do not weaken determinism without
                   measured evidence and a contract change. D1 remains owned by
                   the human. By board order, D2 is the next eligible agent
                   packet; its deploy script must be built/tested but never run
                   by an agent.
