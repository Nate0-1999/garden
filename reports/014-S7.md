packet:            S7 — Broker-routed embeddings
session:           codex / 2026-07-20 / c105
status:            DONE
what exists now:   Spine Settings defaults embeddings to
                   https://openrouter.ai/api/v1 with the namespaced
                   openai/text-embedding-3-small model. create_app passes the
                   configured URL into the existing OpenAI-compatible adapter;
                   the fixed 1536-dimensional boundary is unchanged. Local
                   Compose forwards URL and model overrides and resolves the
                   existing generic bearer slot from an explicit compatible
                   key, then OPENROUTER_API_KEY, then legacy OPENAI_API_KEY.
deviations:        No Garden amendment, HTTP contract change, new adapter/SDK,
                   runtime fake, dimension change, or Harness production edit.
                   Spine DECISIONS 014 (P1.1) records composition at the
                   existing provider seam. SPINE_EMBED_MODEL passthrough and
                   broker-first key fallback accompany the packet's literal
                   base-URL passthrough because otherwise Compose could not
                   execute the promised direct override or default broker
                   route. openapi.json regenerated byte-for-byte unchanged.
evidence:          Spine verification/s7/README.md. Default OpenRouter and
                   direct-provider composition are covered with the existing
                   deterministic scripted provider; adapter validation remains
                   covered by its in-process HTTP transport tests. Spine passes
                   160 tests; Harness passes 220 non-contract tests with 2
                   deselected, and its disposable migrated production-image
                   contract passes 2 tests with clean teardown. Ruff, format,
                   lock, M1 scope, Compose routing sentinels, web lint/build,
                   OpenAPI freshness, diff checks, and two independent audits
                   are green.
notes to the next agent:  Spine remote head is
                   4169d11597187f3371d93831d2e5d47c2329ff11. D1 now has both
                   dependencies and remains the sole active human packet. Give
                   Cloud Run SPINE_EMBED_BASE_URL, SPINE_EMBED_MODEL, and the
                   OpenRouter bearer through the established
                   SPINE_OPENAI_API_KEY slot, then perform D1's required live
                   create → dedup → prepare → commit round trip. The SPEC
                   records prior live broker verification; S7 deliberately
                   used deterministic fakes and did not spend a hosted call.
                   H5 stays blocked until the human marks D1 DONE; no other
                   agent-owned TODO packet is dependency-ready. Stop here under
                   the one-packet rule.
