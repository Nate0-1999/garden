packet:            P0 — Agent Zero
session:           codex / 2026-07-17 / 019f
status:            BLOCKED
what exists now:   Three sibling git repos exist with Garden operations and
                   exact P0 product scaffolds; no M1 business logic was built.
                   Spine commit cea4367 contains the full C.2 migration/seed,
                   authenticated health route, seven named 501 stubs, CI, and
                   a tracked scope fence. Harness commit 500975e contains the
                   strict C.7 seam, typed C.4 client stubs, daemon, shell, CI,
                   and the same fence. Both freeze SPEC v1.4 byte-for-byte.
deviations:        Spine DECISIONS 001–002 [P4]; Harness DECISIONS 001–002
                   [P4]. No contract deviation was implemented. F001–F005
                   record contract ambiguities that P0 refused to guess at.
evidence:          spine/verification/bootstrap/README.md (14 tests, clean
                   Alembic/pgvector Compose boot, seed trace, all seven live
                   501s); harness/verification/bootstrap/README.md (18 pass,
                   1 intentional H2 skip, live WS echo, web lint/build, and
                   390x844 browser trace plus screenshot). Both staged scope
                   fences passed and rejected the forbidden-feature probes.
notes to the next agent:  P0's executable exit checks are green, but the relay
                   must not advance while F001–F005 are open. The human must
                   resolve the flags (normally by amending the spec) and reset
                   P0 to TODO; a fresh P0 session should then update the frozen
                   copies/contracts as required and re-verify before DONE.
                   Planting begins at C.2 rules, then C.4 memory endpoints.
                   No repo has a remote, so the local commits could not be
                   pushed; do not mistake that for a completed push ritual.
                   Run git inside each child repo: the workspace root also has
                   a pre-existing inert .git directory with no commits.
                   On this Colima workstation, testcontainers needs
                   TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock.
                   If macOS hides an editable-install .pth and imports fail,
                   rebuild with `uv venv --clear --python 3.12` then sync.
