packet:            S4 — Commit, feedback, quarantine
session:           codex / 2026-07-19 / a4c9
status:            DONE
what exists now:   Spine commit validates and atomically applies the full gate
                   batch, writes exactly-once outcomes and CAS-backed stats,
                   returns current post-stat wrong removals, and renders the
                   exact frozen final block. Never decisions use their event's
                   scorer version and quarantine at its threshold. Feedback is
                   an exactly-once event state machine; mid-thread removal has
                   CAS lineage and cited remains M1 log-only. OpenAPI exposes
                   both live endpoints and only /v1/search remains a 501 stub.
deviations:        DECISIONS 010 (P1.2.1a, P1.2.1b, P1.2.1d) and 011
                   (P1.2.1c), under enacted Garden A-009 through A-011.
evidence:          Spine verification/s4/README.md; 79 pytest checks passed
                   against disposable pgvector Postgres through migration
                   0002. Ruff lint/format, uv lock, scope fence, Compose config,
                   committed OpenAPI freshness, and production Docker image
                   passed. Harness: 27 passed with the one intentional H2
                   contract skip; Ruff lint/format, uv lock, and scope fence
                   passed. Independent read-only audits found no blocker.
notes to the next agent:  S4 remote head is
                   ddb5e21df39d0ec1909791a017f45890d5a27f73. D1 is now the
                   dependency-ready row, but it is explicitly HUMAN-only:
                   deploy this Spine image to Cloud Run + Cloud SQL, migrate
                   through 0002, then remotely verify health and a real-embed
                   create → dedup → prepare → commit round trip before marking
                   D1 DONE. Do not have a relay agent claim it. Commit renders
                   only event features._memory; wrong_removed deliberately
                   serializes the current head. An empty unknown injection UUID
                   succeeds only when both choice lists are empty because S3
                   has no batch row for a zero-card prepare. Untouched near
                   misses remain NULL. cited writes no memory stat/revision;
                   mid_thread_removed increments removals once. Harness's live
                   contract test is still the intentional H2-owned skip.
